# Домены / IP / подсети, добавляемые сообществом. Одна запись на строку, причина — после '#'.
# Формат как во внешних списках: example.com, 203.0.113.0/24, ||site^ .
# Ниже — демонстрационные seed-записи (безвредные примеры); настоящие правки придут PR-ами.
example.org
203.0.113.0/24
