# Локальные правила цели proxy (open-world-filter).
# Основная масса доменов/IP приходит из источников forge.toml
# (генерируется командой 'python -m pipeline emit').
# Сюда — точечные правила и исключения:
#   domain:example.com      точный хост
#   suffix:example.org      домен и поддомены
#   1.2.3.0/24              подсеть
#   !suffix:bad.example     исключение (вырезается после слияния источников)
